@* @model DataAccessLayer.ViewModels.OrderViewModel

<div class="modal-content">
    <div class="modal-header">
        <h5 class="modal-title fs-5 fw-bold text-secondary">@(Model.OrderId == 0 ? "Place Order" : "Edit Order")</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
    </div>
    <div class="modal-body">
        <form id="SaveOrderForm">
            <input type="hidden" asp-for="OrderId" />
            <input type="hidden" id="itemId" asp-for="ItemId" />
            <div class="mb-3">
                <label asp-for="CustomerName" class="form-label">Customer Name</label>
                <input asp-for="CustomerName" class="form-control" required />
                <span asp-validation-for="CustomerName" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="ItemName" class="form-label">Item Name</label>
                <input id="itemName" asp-for="ItemName" class="form-control" required autocomplete="off" />
                <span asp-validation-for="ItemName" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Quantity" class="form-label">Quantity</label>
                <input type="number" id="quantity" asp-for="Quantity" class="form-control" min="1" required />
                <span asp-validation-for="Quantity" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Price" class="form-label">Price per Unit ($)</label>
                <input type="number" id="price" asp-for="Price" class="form-control" readonly />
            </div>
            <div class="mb-3">
                <label asp-for="OrderAmount" class="form-label">Total Amount ($)</label>
                <input type="number" id="orderAmount" asp-for="OrderAmount" class="form-control" readonly />
            </div>
            <div class="mb-3">
                <label asp-for="OrderDate" class="form-label">Order Date</label>
                <input type="date" asp-for="OrderDate" class="form-control" required />
                <span asp-validation-for="OrderDate" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="DeliveryDate" class="form-label">Delivery Date</label>
                <input type="date" asp-for="DeliveryDate" class="form-control" required />
                <span asp-validation-for="DeliveryDate" class="text-danger"></span>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-primary">Save</button>
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
            </div>
        </form>
    </div>
</div>

<script>
    $(function() {
        $("#orderDate").on("change", function() {
            const orderDate = new Date($(this).val());
            const minDeliveryDate = new Date(orderDate.setDate(orderDate.getDate() + 15));
            $("#deliveryDate").attr("min", minDeliveryDate.toISOString().split("T")[0]);
        });
    });
</script> *@

@model DataAccessLayer.ViewModels.PaginationViewModel<DataAccessLayer.ViewModels.ItemViewModel>

<input type="hidden" id="totalRecords" value="@Model.TotalCount" />

@foreach (var item in Model.Items)
{
    <div class="col-12 col-md-6 col-lg-4">
        <div class="card item-card border-0 shadow-sm p-3">
            <div class="card-body">
                <h5 class="card-title fw-bold text-dark">@item.ItemName</h5>
                <p class="card-text text-muted">Price: $@item.Price.ToString("F2")</p>
                <p class="card-text text-muted small">Added: @item.CreatedAt.ToString("MMM dd, yyyy")</p>
                <div class="d-flex gap-2 mt-3">
                    <button class="btn btn-sm btn-primary placeOrder" data-bs-toggle="modal" data-bs-target="#SaveOrderModal" data-item-id="@item.ItemId" data-item-name="@item.ItemName" data-item-price="@item.Price">
                        <i class="bi bi-cart-plus"></i> Order
                    </button>
                </div>
            </div>
        </div>
    </div>
}